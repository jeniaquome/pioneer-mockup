# Railway Environment Variables Template
# Copy these variables to your Railway project settings

# Database Configuration (Railway PostgreSQL)
DATABASE_URL=postgresql://postgres:password@postgres.railway.internal:5432/railway

# Redis Configuration (Railway Redis)
REDIS_URL=redis://redis.railway.internal:6379/0

# Application Configuration
INIT_DEMO_DATA=false
INIT_DEMO_USERS=true
INIT_DEMO_DATA_MODE=no-resources

RECOMMENDER_MODEL=gemini-2.5-flash-preview-09-2025

# Security (Generate these secrets)
SECRET_KEY=your-secret-key-here
JWT_SECRET_KEY=your-jwt-secret-key-here

# Email Configuration (if using email features)
MANDRILL_API_KEY=your-mandrill-api-key
FROM_EMAIL=noreply@yourdomain.com
FROM_NAME=Pioneer Team

# Google AI Configuration (if using AI features)
GOOGLE_AI_API_KEY=your-google-ai-api-key

# Sentry Configuration (for error tracking)
SENTRY_DSN=your-sentry-dsn

# Optional: Custom domain
DOMAIN=your-custom-domain.com

# Rate Limiting Configuration (in-memory storage, no Redis needed)
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=200
RATE_LIMIT_AI_PER_MINUTE=30

# Railway automatically provides:
# - PORT (automatically set by Railway - DO NOT override this)
# - RAILWAY_ENVIRONMENT (production/staging)
# - RAILWAY_PROJECT_ID
# - RAILWAY_SERVICE_ID